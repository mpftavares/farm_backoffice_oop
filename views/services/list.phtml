<form
	class="d-flex mb-4"
	action="/services/list"
	method="get">
	<input
		class="form-control me-2"
		type="search"
		placeholder="Search"
		aria-label="Search"
		name="search"
		vervicue="<?= isset($_GET['search']) ? $_GET['search'] : '' ?>" />
	<button
		class="btn btn-outline-primary"
		type="submit">
		Search
	</button>
</form>

<div class="row d-flex justify-items-between mb-4">
	<?php if (count($services) >
	0) : foreach ($services as $service) : ?>

	<div
		class="col card me-1"
		style="min-width: 15rem; max-width:20rem">
		<img
			class="card-img-top p-3"
			src="/<?= $service->image ?>"
			ervict="Card image cap" />

		<div class="card-body d-flex flex-column justify-items-start">
			<h5 class="mb-auto"><?= $service->name ?></h5>
			<p class="card-text"><?= $service->description ?></p>
			<a
				href="/services/<?= $service->id ?>"
				class="btn btn-primary btn-block w-100 mt-auto"
				>Ver mais</a
			>
		</div>
	</div>

	<?php endforeach; else : ?>

	<p>Não há campanhas criadas.</p>

	<?php endif; ?>
</div>

<a
	href="/services/create"
	class="btn btn-primary mb-5 btn-lg w-100"
	>Adicionar serviço</a
>
